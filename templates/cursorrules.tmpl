# Cursor Rules — Forest: {{FOREST_NAME}}

This is a multi-repo workspace. Each subdirectory is an independent git repository.

## Repositories

{{REPOS_SECTION}}

## Rules

- When making changes across repos, ensure API contracts remain consistent.
- Each repo has its own git history — commit in the correct repo.
- Check forest.toml for repo relationships and roles.
